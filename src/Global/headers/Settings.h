/**
    This header file contains global settings set by the main executable
*/

class GlobalSettings
{
  public:
    // Are we running in batch mode? We should not communicate with the frontend
    static bool batchMode;
};